import React from 'react'
import ReactDOM from 'react-dom/client'
import "@junin-ui/scss-styles/dist/main.scss";
import App from './App.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
